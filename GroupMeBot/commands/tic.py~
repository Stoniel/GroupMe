from core import command
import random

class Tic:
    def __init__(self, bot):
        self.bot = bot
        self.array = [['','',''],['','',''],['','','']]

    @command.command()
    def Tic(self):
        self.counter += 1
        self.bot.send_message(self.)

def setup(bot):
    bot.add_cog(Tic(bot))
